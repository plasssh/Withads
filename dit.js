const addToCartButton = document.querySelector(".add-to-cart-button");
const addedToCartText = document.querySelector(".added-to-cart");

addToCartButton.addEventListener("click", () => {
  // Add logic here to handle adding to cart
  addedToCartText.style.display = "block"; // Show "Added to cart" message
});
    